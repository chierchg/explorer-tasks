{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Project / Step 1: Package",
      "type": "shell",
      "command": "npm run package",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build",
      "problemMatcher": []
    },
    {
      "label": "Project / Run regression tests",
      "type": "process",
      "command": "node",
      "args": [
        "--test"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "test",
      "problemMatcher": []
    }
  ]
}
